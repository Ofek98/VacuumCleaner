{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch myrobot",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/src/myrobot",
            "args": ["-summary_only", "-num_threads=2"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/src",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "build"
            
        }
    ]
}
